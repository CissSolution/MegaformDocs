// ============================================================
// MegaForm Views — Entry Point
// Public form renderer (future migration)
// Currently a placeholder — submissions moved to submissions/ bundle
// ============================================================

// Expose globally
if (typeof window !== 'undefined') {
  (window as any).MegaForm = (window as any).MegaForm || {};
  // Views-specific functions will be added here when form renderer is migrated
}

export {};
