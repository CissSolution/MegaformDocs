# HANDOFF — 2026-06-16 Regression-fix + SDK session

Long autonomous session. Live site = `Oqtane.MSSQL3` @ http://localhost:5070 (host/`abc@ABC1024`). Source-of-truth gold = `E:\DNN_SITES\OqtaneSites\Oqtane_new`. Deploy = copy `Assets/js|css/*` → live `wwwroot/Modules/MegaForm/js|css/`. QA scripts in `tmp-qa/qa-*.cjs` (Playwright, fresh ctx = no cache).

## ✅ DONE + DEPLOYED to live (verified)
1. **Languages 18 locales** (de-DE/pt-BR/ar-SA + it/nl/pl/ru/tr/id/hi/th restored). Root cause: stale `i18n/index.json` (7 entries) + `MegaFormController.ListI18nLocales` short-circuit + live missing 12 packs. Fixed `index.json`→18 in `public/i18n` + `Assets/js/{builder,bundles}/i18n` + deployed packs+index to live & repo wwwroot. Hardened endpoint to UNION index+disk (source; ships next server build). Verified: `curl /api/MegaForm/i18n/list` → 18; Playwright DOM shows restored codes.
2. **Workflow canvas** stale April-21 bundle → copied fresh `megaform-workflow-reactflow.js` (md5 167067b8, `MF240423-APPROVAL-SPLIT`) to live.
3. **Field key** `composite_10_xvjj` → clean `composite`/`composite_2` (collision-checked) + editable hint in properties. `core.ts generateFieldKey` + `fields.ts`/`dom.ts` hint. Built+deployed `megaform-builder.js`. Playwright: new field → `number_2`, hint visible.
4. **Submissions overview re-wire (B162)** — `SubmissionsShell.ts` was April-reverted (lost the forms-overview wiring; `forms-overview.ts` survived orphaned). Re-wired: `_viewMode` overview/list, `renderOverview()`→`renderFormsOverview`, `onPickForm`→drill-in, `buildBackBar()` "← All forms". Badge `SubmissionsShell v20260616-B162rewire`. Built+deployed `megaform-submissions.js`. Playwright DOM: chart + 5 form rows + 29 KPIs, flat-table hidden.
5. **Detail-view structured/composite render (Item A)** — Submission detail sheet (`viewSubmissionDetail`→`mountTaskDetail`→`my-inbox/view.ts renderDetail`) dumped composite values as raw JSON. Added `mfLooksLikeJsonObject`/`mfRenderStructuredValue`/`mfRenderPartValue` → headline + humanized parts (each part by kind: image/html/url/text). Built+deployed (marker `mf-mi3-cell-structured-main` in live bundle). ⚠️ My-Inbox BOARD (`?mfpanel=myinbox`) shares `view.ts` but appears to NOT be a vite entry (only `SubmissionsShell` imports my-inbox) — possible April-revert dropped a `my-inbox` entry; FLAG for follow-up.
6. **DB-tables panel "No tables match"** = NOT a regression (no `DashboardDatabase` connection on MSSQL3 → graceful-degrade). Optional UX: surface the server `warning`.

## ✅ DONE — SDK P2 (Item D), build-verified, NOT a live concern
- **`ISchemaApi.Parse(schemaJson)` / `ParseForm(FormDto)`** on `IMegaFormClient.Schema` → read-only `FormSchemaInfo`/`FormFieldInfo`/`FieldValidationInfo`/`FieldOptionInfo` (mapped via `RenderModelResolver.Resolve().Schema` + `MegaFormUtils.FlattenFields`; no Core-type leak; `string` Type for plugin types; `IsInputField` false for Html/Section/Row/UniqueId). PublicAPI.Unshipped updated. SDK builds net472/8/9/10; **24/24 tests pass** (19 + 5 new). This is the audit's "FormSchema.Parse SDK expose" item.

## ⚠️ CACHE — why the user "couldn't confirm"
Bundle fixes (3,4,5) are deployed but loaded with `?v=` cache stamps. **Source versions BUMPED to `20260616-B172`** (`MegaForm.Oqtane.Client/Index.razor:1003 OqtaneCoreAssetVersion`, `SubmissionsView.razor:68`, `MegaForm.UI/src/loader/index.ts:24 BUILDER_BUNDLE_VERSION`) — but these only take effect after an **Oqtane Blazor Client rebuild + deploy** (NOT done this session — too risky unsupervised). **Until then: HARD-REFRESH (Ctrl+Shift+R) to see all fixes.** Languages picker is unaffected by cache (reads the API live). Playwright `?mfpanel=*` screenshots land on the ACME home in fresh headless (SPA-overlay takeover flaky) — DOM `page.evaluate` is the reliable check, not the screenshot.

## ⬜ REMAINING (ready, NOT done — risky live rebuilds, do supervised; user authorized "rebuild thoai mai")
- **(B) Composite i18n = WHOLE-VALIDATOR** (user-confirmed scope). Localize ALL of `FormValidationService.Validate` (composite + Email/Number/Phone/Date) via optional `ILocalizationProvider loc = null` overload + a fail-soft `Loc(loc,key,englishFallback,args)` helper. Reuse existing `MegaFormStrings.cs` keys (form.required_field/invalid_email/min_length{min}/...); ADD 7 composite keys (form.invalid_number/min_value/max_value/incomplete/match/min_age/max_age) to `MegaFormStrings.cs` + `Assets/js/i18n/en-US.json` (client refs them but they're MISSING). `CompositePresetRegistry.cs` SSN/confirm messages → keys. `SubmissionProcessor.cs:155` pass `_loc`. Placeholder drift: standardize on `{n}` (or pass both n+min+max aliases). Overlay JSONs fall back to English until translated (Translate-AI later). Needs Core DLL rebuild + restart 5070. SAFETY: fail-soft helper never throws; composite already fail-open try/catch.
- **(C) SDK wiring BLOCKER — ✅ SOURCE DONE + BUILD-VERIFIED (NOT deployed to live).** Discovered `MegaForm.Oqtane.Server` had NO ProjectReference to `MegaForm.Sdk` at all (the bin DLL was a stale transitive copy), and `AddMegaFormSdk()` was called nowhere. APPLIED: added `<ProjectReference ..\MegaForm.Sdk\MegaForm.Sdk.csproj>` to the Server csproj; `using MegaForm.Sdk;` + `services.AddMegaFormSdk();` at the end of `ConfigureServices` (Startup.cs); `MegaFormSdk.Initialize(app.ApplicationServices)` (try/catch) in `Configure`. **`dotnet build MegaForm.Oqtane.Server -c Release` = Build succeeded.** The DLL is in the repo bin, NOT copied to live (deferred — unsupervised restart risk + the known Kestrel-rebind quirk). This Server DLL ALSO carries the i18n-controller union-fix. SAFE to deploy: AddMegaFormSdk is TryAddScoped + only resolved on demand (nothing consumes IMegaFormClient yet → can't break startup); Initialize is try/catch. STILL OPTIONAL/future: register an Oqtane `IPlatformContext` impl (11 members) for ambient tenant/user — SDK works without it via explicit `MegaFormScope`. **To finish: copy `MegaForm.Oqtane.Server/bin/Release/net10.0/{MegaForm.Oqtane.Server.Oqtane.dll, MegaForm.Sdk.dll}` → live MSSQL3 root (backup first) → restart → curl-verify.**
- **(P3 + docs)** IFormRenderer/Blazor POC (Strategy B hybrid) + Writing-Data dev doc + Blazor doc (audit gaps). Designs in `Docs/FUTURE_PLAN_MEGAFORM_SDK_AND_DOCS.md` §E/F + the wf_fa1f2aa5 roadmap output.
- **Cache deploy:** rebuild Oqtane Blazor Client (picks up B172) + redeploy → user sees fixes without hard-refresh. Also rebuild `loader` (npm) for the builder ?v=.

## Deploy recipe (when supervised)
`dotnet build MegaForm.Core` + `MegaForm.Oqtane.Server` (+ Blazor Client) → backup live DLLs → copy DLLs to live root → `Stop-Process Oqtane.Server` → `Start-Process MSSQL3\Oqtane.Server.exe` → `curl /api/MegaForm/i18n/list` (expect 200/18) → rollback DLLs if down. JS/CSS: `npm run build:<entry>` → copy `Assets/js|css/*` → live `wwwroot/Modules/MegaForm/js|css/`.
