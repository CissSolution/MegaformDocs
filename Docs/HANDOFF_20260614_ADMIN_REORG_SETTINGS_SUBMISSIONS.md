# Handoff — 2026-06-14 — Admin shell reorg: Settings consolidation (DONE) + Submissions WPForms-style (IN PROGRESS) + queued tasks

Session = reorganising the MegaForm Oqtane admin shell per a series of user directives. **Constraints that apply to ALL of this work:** (1) **NO hard-coded UI text — use i18n** `T(key, fallback)` (keys in `MegaForm.UI/public/i18n/en-US.json`); (2) **don't let any TS file grow too large** — `dashboard/index.ts` (~4600 lines) should be split; put new code in new small files.

Live host: **Oqtane_new :5000** (`E:\DNN_SITES\OqtaneSites\Oqtane_new`, host/Minh@2002). ⚠️ **E: is an external USB SSD (Samsung T7) that has disconnected mid-session before** — it corrupted 3 MegaForm.Core files last time (`?.[`→`?[`, now fixed); if a build fails with odd syntax errors in untouched files, `grep -rn '?\.\[' --include=*.cs`.

---

## ✅ TASK 1 — Settings consolidation (DONE + built + deployed + QA-proven)
**Goal:** the 7 separate settings nav items (Database/Payment/Email/Upload/Captcha/AI/Google Sheets) → ONE "Settings" pane with tabs. **Languages stays a separate Configuration item (NOT a tab).** Don't break the working settings code.

**How (all in `MegaForm.UI/src/dashboard/index.ts`):**
- New `settingsHost(targetBody, title, icon, loading, maxW): HTMLElement` (just before `openDatabaseSettings`). In standalone mode → `modal(...)` (real overlay). In TAB mode (targetBody passed) → a tiny **fake-ov** whose `querySelector('.mf-modal-body')` returns `targetBody` and whose `remove()` is a no-op — so every existing `ov.querySelector('.mf-modal-body')` line in the 7 functions works UNCHANGED, and a Save's `ov.remove()` no longer closes the whole pane.
- New `openSettingsPane(initialTab?)`: a modal with a LEFT tab rail (7 tabs) + a race-safe content host (a fresh `.mf-settings-tab-body` div per tab select). Each tab calls `open*Settings(contentDiv)`.
- The 7 `open*Settings()` each got a `targetBody?: HTMLElement` param + their `const ov = modal(...)` became `const ov = settingsHost(targetBody, ...)` (GoogleSheets builds content sync → `if(targetBody){append} else {modal()}`). **All load/save logic untouched.**
- Tab labels reuse existing i18n keys (`dash.nav_database`, `dash.nav_payment`, …). New keys added to en-US.json: `dash.nav_settings`, `dash.nav_form_management`, `dash.settings_title`.
- Icon: there was no `settings` icon → used `gear` (added the gear SVG to the `submissions/SubmissionsShell.ts` + `languages/index.ts` icon maps; dashboard already had it).

**QA-proven** (`MegaForm.UI/tools/scn-settings-qa.cjs`): nav shows Form Management + Settings + Languages (the 7 old items GONE); clicking Settings opens the pane with **7 tabs** [Database/Payment/Email/Upload/Captcha/AI/Google Sheets]; tabs render content (Database 5818 chars, Payment 6860) and switching works; 0 errors. Screenshot `tmp-qa/settings-pane.png`.

## ✅ TASK 2 — Nav reorg (DONE, in all 3 nav files)
- "Dashboard" → **"Form Management"** (i18n `dash.nav_form_management`), moved to the **bottom of the Main group** (Main = Form Builder · Submissions · My Inbox · Form Management).
- Configuration = Languages + **Settings** (one item). The dashboard Settings item is `onClick:()=>openSettingsPane()`; the Submissions/Languages sidebars deep-link to `dashboard#settings`.
- Hash routing in `dashboard/index.ts` (~end of render): `#settings` + legacy `#database-settings`/`#payment-settings`/… all map to `openSettingsPane(tab)`.
- Edited: `dashboard/index.ts` (nav array + breadcrumb), `submissions/SubmissionsShell.ts` (nav block), `languages/index.ts` (nav template).

**Deployed:** rebuilt + synced `megaform-{dashboard,submissions,languages,i18n}.js` + `megaform-admin-shell.css`; copied the 4 JS + CSS to `Oqtane_new\wwwroot\Modules\MegaForm\{js,css}`. **NO `?v=` stamp bump** → a REAL browser needs **Ctrl+F5** to see it (headless QA uses no-cache so it already proved out). To make it stick for normal loads, bump `Index.razor` B161→B162 + rebuild the Client DLL — but that also ships the pending flash-fix (see bottom), so decide deliberately.

---

## ✅ TASK 3 — Submissions WPForms-style (DONE + QA-proven 2026-06-14, B162)
**Delivered:** pixel-ported the v0 mock (`mega-form-admin-redesign (3)`, live :3000/submissions) onto the real Submissions surface.
- **Backend fixed:** `Reports/FormsOverview` was returning 0 forms (`ResolvePortalId()`→0). Now takes `siteId` + reads through `_formRepo.ListForms(siteId, pageSize:0)` like Form/List → 49 forms, 3 with subs. Server rebuilt+deployed (+ also fixed the earlier TypeLoad crash by syncing Core/Shared/Sdk DLLs to the site).
- **New file** `src/submissions/forms-overview.ts` (self-contained, i18n) — KPI strip + "Submission Volume" inline-SVG area chart (7D/14D/30D) + forms table (Star · Form Name+#id · Status badge · Created · All Time · Last 7d · Last 30d · Completion bar · Trend 14d sparkline · Actions). Derives last7/last30/trend/sparkline from the 30-day series; **completion = REAL field-completion** (backend `ComputeFormCompletion`: avg over a form's non-spam submissions of filled-input-fields/total-input-fields, from schema + dataJson; null→"—" for empty forms). Live: Corporate Contact 95%, Bug Report 78%, Tech Summit 100% (user chose "real backend metric" over the 100%/— placeholder).
- **Wired** into SubmissionsShell.ts: `_viewMode` overview↔list, drill-in via `onPickForm`→`switchForm`, "← All forms" back bar.
- **Geist font** (the "sharpness" win): admin-shell.css `@import` Google Fonts Geist + `--font` leads with Geist (applies to the WHOLE dashboard). The inline-surface isolation reset in Index.razor hard-pinned Inter → added a `!important` Geist override for `.mf-oq-surface.is-inline` in admin-shell.css (works without a Client rebuild; Index.razor source also updated for the next build).
- **CSS** appended to `megaform-submissions-ts.css` (`.mf-fo*` + `.mf-subs-backbar`), measured 1:1 from the mock's computed styles.
- **QA-proven (mf-hb + screenshots):** overview renders (4 KPIs, chart polyline, 9 cols, 49 rows, sparklines, Geist loaded `document.fonts.check('16px Geist')=true`); drill-in Corporate Contact → its 10 submissions + back bar + all old features (Manage Columns, status badges, pagination, detail). Scenarios: `tools/scn-formsoverview.cjs`, `scn-subs-overview.cjs`, `scn-subs-fs.cjs`, `scn-subs-drill.cjs`, `scn-mock-capture.cjs`. Shots in `tmp-qa/oq-subs-{fs,inline,drill}.png` vs `mock-subs.png`.
- **Deployed** to Oqtane_new: `megaform-submissions.js` + `megaform-admin-shell.css` + `megaform-submissions-ts.css`. NO `?v=` bump → Ctrl+F5.
- **Known minor deviations from the mock (acceptable / honest):** no dark unsplash hero banner (our compact topbar instead); row sub-line = `#id` not a category (no category data); KPI sub-text "Recent activity" not "+8% vs prev"; nav kept as the earlier-agreed reorg (Form Management + single Settings), NOT the mock's older nav. Surface is inline-by-default with a Fullscreen toggle (`?mfinline=0` or the "Windowed" button) — fullscreen is the clean mock-like view.
- **DNN parity TODO:** add `Reports/FormsOverview` to `MegaForm.DNN` (the TS auto-applies once the endpoint exists).

## TASK 3b — Submission detail polish + Forward audit (2026-06-14, B163)
User asks: (1) hide ID+Form columns by default + reuse the polished My-Inbox detail for a submission's detail; (2) Forward must update History/Workflow + leave an audit trail.

- **✅ 3b-A — ID + Form columns hidden by default (DONE + QA-proven).** `SubmissionsShell.ts`: `id`→removable:true, new `DEFAULT_ACTIVE_KEYS=['name','date','status']` + `defaultActiveColumns()`, storage key bumped `mf-subs-columns-v2`→`v3` (existing users reset to the new default). QA: drill-in header = Submitted By/Date/Status, "3 columns shown", ID+Form gone (still addable via Manage Columns). `tools/scn-subs-cols.cjs`.
- **✅ 3b-B — Forward audit (History/Workflow update) (DONE, code+backend-confirmed, deployed).** ROOT CAUSE was frontend-only: `my-inbox/index.ts submitReply` reloaded the board after forward (task leaves my queue → detail blanks, History never shows the forward). Backend already persists it (`WorkflowTaskService.ForwardTaskAsync`→`AddTaskAction`→`MF_WorkflowTaskActions`; `BuildWorkflowTaskPayload`→`ListTaskActions` returns the FULL action list). FIX: `workflow-inbox/api.ts` action wrappers now RETURN the task detail (`postTaskAction`→`normalizeTaskDetail`, void→`WorkflowInboxTaskDetail`); `index.ts` adds `detachedTask`/`detachedStatus` (keep the just-actioned task on screen after it leaves the queue) + `refreshDetailFromResponse(sid, resp)` (rebuild the cached detail from the response's authoritative action list — History shows "Forwarded to X", Workflow reflects the new assignee). Applied to approve/reject/forward AND comment/return. `mapHistory` reads ONLY the passed `actions`, and the response carries them all → correct.
- **✅ 3b-C — Submission detail REUSES the My-Inbox detail panel (DONE + QA-proven, single source of truth).** Clicking a submission row now opens the SAME polished detail (avatar + FORM RESPONSES 2-col rich-render + Details/History/Workflow tabs + action bar) instead of `renderSubmissionDetailShell`. NEW single-source host `my-inbox/standalone-detail.ts` `mountTaskDetail(container, {config, submissionId, formId, formTitle, onChanged})` — REUSES (no duplication) `buildDetailPanel` (now exported from view.ts), `adaptTask`+`buildEnrichedDetail`, `createWorkflowInboxApi`; holds a thin local state machine + wires actions to the workflow API with the same response-refetch (Forward audit works here too). Gets the task/actions via `api.getTask(taskId)` (taskId from `detail.workflowDetail.workflowTasks`); plain submissions (no workflow) → synthetic task, `hideTaskActions` (new board-safe BoardContext flag) → Export-only. Self-injects `megaform-my-inbox-ts.css` (added to submissions vite CSS_MAP + ensureInboxCss `<link>` inject — Razor Resources don't load it). `SubmissionsShell.viewSubmissionDetail` swaps to `mountTaskDetail` with `renderSubmissionDetailShell` as fallback. QA: `scn-subs-detail.cjs` (6 field cells, tabs, Export-only for no-task), board still full actions (`scn-myinbox-audit.cjs`). Deployed (submissions.js 290KB + my-inbox.js + my-inbox CSS).
- **(superseded) 3b-C plan** Goal: clicking a submission row opens the polished Inbox detail (avatar + FORM RESPONSES rich-render + Details/History/Workflow tabs + Approve/Reject/Return/Forward/Comment/Export) instead of the current `renderSubmissionDetailShell` (Data/Form/DB/Flow/Activity sheet). PLAN (scouted): (1) `export` `buildDetailPanel` from `my-inbox/view.ts`; (2) add `megaform-my-inbox-ts.css` to the `submissions` vite CSS list (vite.config.ts); (3) new `submissions/inbox-detail-host.ts` = a standalone host that keeps local state {item, activeDetailTab, replyMode, forwardTarget, directory, busy} + builds a full `BoardContext` (board-list callbacks as no-ops; detail callbacks wired to `createWorkflowInboxApi` actions + `refreshDetailFromResponse`-style refetch so Forward audit works here too) and re-renders `buildDetailPanel` into the sheet body; (4) map a submission→`InboxTaskItem` via `adaptTask`+`buildEnrichedDetail` — the workflow task/actions come from `detail.workflowDetail` (WorkflowTasks/WorkflowActions); handle the NO-workflow-task case (show fields + a "Submitted" history, collapse the action bar via a non-pending status); (5) swap in `viewSubmissionDetail` (SubmissionsShell.ts:1123), keep `renderSubmissionDetailShell` as fallback. Key reuse facts: `buildEnrichedDetail(detail, actions, base)` → fields+history; CSS `.mf-mi3-*`; `buildActionBar` shows actions when status∉(approved/rejected/done).

## ⏳ TASK 3 (orig notes) — Submissions WPForms-style
**Goal:** clicking **Submissions** shows a WPForms-style **forms overview** = a table of forms with **Form Name · Created · All Time · Last 7 days · Graph (sparkline)** (+ optionally a top trend chart). Clicking a form → that form's submissions list (the existing table). Form-first.

**Data finding (why a backend endpoint was needed):** `Form/List` returns NO `submissionCount` and `createdOnUtc` is min-date (`0001-01-01`); the Submissions endpoint with `formId=0` returns 400. So per-form metrics can't be computed client-side.

**DONE (backend):** added **`GET /api/MegaForm/Reports/FormsOverview?days=7`** to `MegaForm.Oqtane.Server/Controllers/MegaFormController.Reports.cs` (after `ReportsList`). Returns `{ days, generatedAtUtc, forms: [{ formId, title, status, createdOnUtc(nullable), allTime, last7, series:[int × days] }] }`, ordered by allTime desc. Uses `_dbContextFactory` + `db.Forms.Where(PortalId==pid)` + `db.Submissions` GroupBy (exclude IsSpam); series = daily buckets for the last `days`. **Server BUILT (0 errors) + deployed** (`MegaForm.Oqtane.Server.Oqtane.dll` → Oqtane_new root) + Oqtane restarted — **but the endpoint smoke-test (`tools/scn-formsoverview.cjs`) didn't run because the site was still warming when the user paused.** FIRST STEP on resume: confirm Oqtane is up (`Invoke-WebRequest http://localhost:5000/`), then run `node tools/mf-hb.cjs --eval tools/scn-formsoverview.cjs` → expect status 200, formCount≈49, a couple forms with allTime>0 + a 7-int series.

**TODO (frontend, NOT started):**
1. New file **`MegaForm.UI/src/submissions/forms-overview.ts`** (keep it OUT of SubmissionsShell to honour the "small files" rule): `buildFormsOverview(state, onPickForm)` → fetches `Reports/FormsOverview?days=7`, renders a card/table: columns **Form Name | Created | All Time | Last 7 days | Graph** (inline-SVG sparkline from `series[]`, like `dashboard/submission-report.ts` donuts — NO chart lib). Optionally a top area chart of total submissions/day. Use `T(key, fallback)` for every label (new `subs.*` keys → add to en-US.json).
2. Wire into `SubmissionsShell.ts`: when no form is selected (the shell's all-forms state), render `buildFormsOverview` INSTEAD of the flat submissions table (Explore said insert before `buildStats`, ~line 404). Clicking a form row calls the shell's existing form-switch (sets `state.config.formId` / `switchForm`) → the current submissions table for that form + a "← All forms"/breadcrumb to return to the overview.
3. The submissions surface mounts at `?mfpanel=submissions`. The API base is `/api/MegaForm/`. Reuse the shell's existing fetch/auth (`dnnAuthHeaders`-style) — check how SubmissionsShell calls `Submissions?formId=…` and mirror it for the Reports call.
4. Build `node scripts/build-entry.cjs submissions`, deploy `megaform-submissions.js` → Oqtane_new, QA (overview table renders with counts/sparklines; click form → its submissions; back to overview).

**DNN parity (follow-up):** the nav + settings-pane TS is shared so DNN gets it automatically, BUT DNN needs its own `Reports/FormsOverview` endpoint (mirror in `MegaForm.DNN/WebApi/ReportApiController.cs`) for the overview to load on DNN. Also note: the survey below says Oqtane is missing `ModuleConfig/CaptchaSettings|PaymentSettings|EmailSettings` endpoints — the consolidated Settings pane's Captcha/Payment/Email tabs may 404 on Oqtane (verify; Web/DNN have them).

---

## PENDING TASKS (queued by the user)
- **TASK 4 — Split `dashboard/index.ts`** (~4600 lines) into small modules (e.g. `dashboard/settings/*` for the 7 settings functions + `openSettingsPane`, `dashboard/nav.ts`, `dashboard/forms-list.ts`). Risky refactor (shared helpers `modal/field/input/ic/T`); do it carefully with a build+QA after each extraction. The settings cluster (settingsHost + openSettingsPane + the 7 open*Settings + the form helpers field/input/select/toggle/…) is the natural first module.
- **TASK 5 — Integration-providers survey assessment + handoff (NO CODE).** The user pasted a survey: new integration providers (Marketing: Mailchimp/ConvertKit/Brevo/Klaviyo; SaaS: Slack/Twilio/Zapier; Storage: Google Drive/Calendar; Conversion: Abandonment/Lead/Journey/Landing; Email Summaries; Templates Catalog; Quiz) are **backend-ready + DI-registered on Oqtane/Web/DNN but have NO admin UI / no workflow nodes / no field-builder widgets**, and **Oqtane is missing `ModuleConfig/{Captcha,Payment,Email}Settings` endpoints** (Web/DNN have them). Deliverable = a quick assessment + a handoff doc summarising current state + the proposed next steps (build Dashboard→Integrations tabs for API keys; add workflow nodes; add Quiz/LandingPage/EmailSummary field/widget UI; add the 3 missing Oqtane endpoints; provider unit tests). The `DnnServiceLocator.cs` (recently edited) now exposes `MarketingIntegration/SaasAutomation/RecurringPayments/StorageIntegration/Captcha/ConversationalForms/...Quiz` — confirms the DI surface. **No code — just the assessment + handoff.**
- **FLASH FIX (carried over, not built/deployed):** `Index.razor` has a process-static `_formLoadCache` + `TryFastPathFormLoad()` to kill the prerender double-render flash (form→spinner→form). Code is in but the Client DLL was NOT rebuilt/deployed. See `Docs/HANDOFF_20260614_MYINBOX_FIX_AND_SSR_FORMLOAD.md`. Site prerender (`Site.Prerender=1`, SiteId 1) is ON. When the Client is next rebuilt (e.g. for a `?v=` bump), this ships too — verify the flash is gone (Ctrl+F5 aurora).

## Build/deploy quick-ref
- TS bundle: `cd MegaForm.UI && node scripts/build-entry.cjs <dashboard|submissions|languages|i18n>` → sync-platforms writes to repo `Assets/` + Oqtane/DNN/Web wwwroot copies (NOT live sites). Then copy `Assets/js/<bundle>.js` → `E:\DNN_SITES\OqtaneSites\Oqtane_new\wwwroot\Modules\MegaForm\js\` (CSS → `…\css\`). JS is static → no Oqtane restart needed; real browsers need Ctrl+F5 (or a `?v=` bump in Index.razor + Client rebuild).
- Oqtane Server: `dotnet build MegaForm.Oqtane.Server/...csproj`; deploy `bin/Debug/net10.0/MegaForm.Oqtane.Server.Oqtane.dll` → Oqtane_new root; `Stop-Process Oqtane.Server; Start-Process Oqtane_new\Oqtane.Server.exe --urls http://localhost:5000`.
- QA: `node MegaForm.UI/tools/mf-hb.cjs --eval <scn>.cjs` (playwright-core, fresh no-cache ctx, in-process login host/Minh@2002). New scenarios this session: scn-settings-qa, scn-formlist-fields, scn-formsoverview.

Related memory: `project_submissions_shell_redesign_b104`, `project_form_ssr_seo_refactor`, `reference_megaform_build_deploy`.
