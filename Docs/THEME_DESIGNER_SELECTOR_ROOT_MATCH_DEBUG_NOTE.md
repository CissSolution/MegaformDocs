# Theme Designer selector root match debug

Patched inspector selectors to target both `[id^="mf-form-wrapper"]` and `.mf-form-wrapper`, added match-count/computed-style debug, and synced TS source to runtime JS copies.
